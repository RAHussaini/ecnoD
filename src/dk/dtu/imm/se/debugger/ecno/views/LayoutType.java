package dk.dtu.imm.se.debugger.ecno.views;

import org.eclipse.zest.layouts.LayoutAlgorithm;

// any enums  created by programmer are direct chilled java.language.Enum   class.   
// enum can not extend another enum. we can not use the keyword 'extends'.
// every enum is final implicitly and we can not create chilled enum. inheritance concept not applicable for enum
// no other normal class would be able inherites from enum, becuase enums are final 
// anyway enum can implement any number of interfaces
public  enum LayoutType {

		SPRING, GRID, HORIZONTAL_TREE, RADIAL, TREE			
		

}
